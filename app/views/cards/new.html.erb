<header>
<h1><%= @deck.name %>: Create a Card</h1>
</header>
<div class="card-form">
  <%= form_for([@deck, @card]) do |form| %>
    <div>
      <%= render partial: "form", locals: { form: form } %>
      <%= form.submit "Create", class: "btn btn-success" %>
    </div>
  <% end %>
</div>
